# Dependencies
node_modules/

# Environment variables — NEVER commit
.env

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Build
dist/
build/

# .env.proof holds the WhatsApp app secret + admin key for scripts/prove-channels.js.
# NOTE: a bare ".env" does NOT match ".env.proof" — it needs its own pattern, or a real secret gets committed.
.env.*

# generated by scripts/make-test-media.js — regenerate, never commit (8.6MB of noise)
test-media/
